import { FilterPipeColorsPipe } from './filter-pipe-colors.pipe';

describe('FilterPipeColorsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeColorsPipe();
    expect(pipe).toBeTruthy();
  });
});
