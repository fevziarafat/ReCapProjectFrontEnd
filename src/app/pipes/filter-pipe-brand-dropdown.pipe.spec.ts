import { FilterPipeBrandDropdownPipe } from './filter-pipe-brand-dropdown.pipe';

describe('FilterPipeBrandDropdownPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeBrandDropdownPipe();
    expect(pipe).toBeTruthy();
  });
});
