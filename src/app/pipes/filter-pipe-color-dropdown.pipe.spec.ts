import { FilterPipeColorDropdownPipe } from './filter-pipe-color-dropdown.pipe';

describe('FilterPipeColorDropdownPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeColorDropdownPipe();
    expect(pipe).toBeTruthy();
  });
});
