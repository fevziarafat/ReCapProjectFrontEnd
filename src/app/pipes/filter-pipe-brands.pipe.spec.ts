import { FilterPipeBrandsPipe } from './filter-pipe-brands.pipe';

describe('FilterPipeBrandsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeBrandsPipe();
    expect(pipe).toBeTruthy();
  });
});
